default:
  endpoint: http://localhost:7200/repositories/<repository_id>

springdoc:
  swagger-ui:
    operations-sorter: alpha 
    tags-sorter: alpha
  packages-to-scan: org.amalic.rdfservices.service

logging:
  level:
    org:
      amalic: INFO
      springframework: ERROR

server:
  port: 8080